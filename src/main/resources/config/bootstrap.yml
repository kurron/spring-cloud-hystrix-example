spring:
    application:
        name: ${name}
    cloud:
        config:
            failFast: true
            discovery:
                enabled: true
                serviceId: jvm-guy-configuration-server
        consul:
            host: 192.168.1.227
            port: 8500
            discovery:
                tags:
                - ${version}
